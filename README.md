# Consulting Website Setup

This repository contains the local setup for the consulting website.

## Requirements
- Docker and Docker Compose

## Quick Start
1. Copy `.env.example` to `.env` and fill the variables (default values are suitable for local dev).
2. Run `docker compose up -d --build`.
3. Frontend: `http://localhost`
4. CMS: `http://localhost:8080`
